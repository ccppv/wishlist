# 🚀 Быстрый старт

## Моментальный запуск (Quick Start)

```bash
# 1. Перейдите в директорию проекта
cd /Users/mansik/Desktop/hh_works

# 2. Запустите инициализацию (автоматически настроит все)
chmod +x init.sh
./init.sh
```

После выполнения `init.sh`:
- ✅ Создаются все .env файлы
- ✅ Генерируется безопасный SECRET_KEY
- ✅ Собираются Docker контейнеры
- ✅ Запускаются все сервисы
- ✅ Применяются миграции БД

## Доступ к приложению

После запуска открывайте в браузере:

| Сервис | URL |
|--------|-----|
| **Frontend** | http://localhost:3000 |
| **Backend API** | http://localhost:8000 |
| **API Docs** | http://localhost:8000/docs |
| **ReDoc** | http://localhost:8000/redoc |
| **Health Check** | http://localhost:8000/health |

## Полезные команды

```bash
# Посмотреть все доступные команды
make help

# Посмотреть логи
make logs              # Все сервисы
make logs-backend      # Только backend
make logs-frontend     # Только frontend

# Остановить
make down

# Перезапустить
make restart

# Создать миграцию
make migration message="описание изменений"

# Применить миграции
make migrate
```

## Структура проекта

```
hh_works/
├── backend/          # FastAPI приложение
├── frontend/         # Next.js приложение
├── nginx/           # Nginx конфигурация
├── docker-compose.yml    # Development окружение
├── docker-compose.prod.yml  # Production окружение
├── Makefile         # Удобные команды
└── init.sh          # Скрипт инициализации
```

## Что дальше?

1. **Изучите документацию:**
   - [README.md](README.md) - полная документация
   - [DEVELOPMENT.md](DEVELOPMENT.md) - гайд по разработке
   - [API.md](API.md) - API документация
   - [DEPLOYMENT.md](DEPLOYMENT.md) - деплой в production

2. **Начните разработку:**
   - Backend: добавьте модели в `backend/app/models/`
   - Frontend: создайте компоненты в `frontend/components/`
   - API: добавьте endpoints в `backend/app/api/v1/endpoints/`

3. **Запустите в production:**
   ```bash
   docker-compose -f docker-compose.prod.yml up -d
   ```

## Требования

- Docker 20.10+
- Docker Compose 2.0+
- 4GB RAM минимум
- 10GB свободного места на диске

## Поддержка

- 📖 [Documentation](README.md)
- 🐛 [Report Bug](https://github.com/yourrepo/issues)
- 💡 [Request Feature](https://github.com/yourrepo/issues)

## Лицензия

MIT - см. [LICENSE](LICENSE)

---

**Готово! Начинайте разработку 🎉**
